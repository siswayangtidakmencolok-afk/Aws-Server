{
  "name": "nusantara-telemedicine-backend",
  "version": "1.0.0",
  "description": "Nusantara Telemedicine Portal - Serverless Backend",
  "private": true,
  "scripts": {
    "test": "echo \"No tests configured\" && exit 0",
    "local-api": "sam local start-api --docker-network telemedicine-network",
    "deploy": "sam build && sam deploy --guided"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "jsonwebtoken": "^9.0.2",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "@aws-sdk/client-sqs": "^3.700.0",
    "@aws-sdk/client-sns": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/client-ssm": "^3.700.0",
    "@aws-sdk/client-apigatewaymanagementapi": "^3.700.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
